/**
 * Provides the classes that represent "scenes". These are fully comparable to "scenes" in Unity or "rooms" in Game Maker.
 * The scene that is referenced by the active {@link rendering.IRenderer} is rendered on the display.
 */
package scenes;

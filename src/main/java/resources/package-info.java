/**
 * Provides the classes to load resources from the disk. These classes use an {@link filesystem.IFileSystem} to gain access
 * to these resources and are able to parse these to the format the other game entities can use.
 */
package resources;

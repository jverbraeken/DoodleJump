/**
 * Provides the classes that should be used to play audio. It is possible that on a different OS instead of the default
 * {@link resources.audio.AudioManager} another implementation of {@link resources.audio.IAudioManager} must be used.
 */
package resources.audio;

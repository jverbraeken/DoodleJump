package objects.powerups;

import objects.IJumpable;

/**
 * Defines a powerup that won't change when the player has jumped on it.
 */
public interface IJumpablePowerup extends IPowerup, IJumpable {
}

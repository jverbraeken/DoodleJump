package objects.powerups;

/**
 * A public enum specifying the type of a passive powerup.
 */
public enum PassiveType {

    /**
     * A passive that applies all the time.
     */
    constant,

    /**
     * A passive that only applies on collisions.
     */
    collision

}

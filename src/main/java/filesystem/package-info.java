/**
 * Provides the classes that should be used to gain access to the file system. When switching to another OS it's
 * possible you have to swap the default {@link filesystem.FileSystem} with another class implementing
 * {@link filesystem.IFileSystem}.
 */
package filesystem;

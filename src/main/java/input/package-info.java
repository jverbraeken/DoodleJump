/**
 * Provides the classes that should be used to process the user input. When switching to another OS it's possible
 * you have to swap the default {@link input.InputManager} with another class implementing {@link input.IInputManager}
 */
package input;
